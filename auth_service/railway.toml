[build]
builder = "DOCKERFILE"
dockerfilePath = "auth_service/Dockerfile"

[deploy]
startCommand = "alembic -c migrations/alembic.ini upgrade head && uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/auth/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
